server:
  port: 8762
  servlet:
    context-path: /eureka-server

spring:
  application:
    name: eureka-server
  zipkin:
    base-url: http://localhost:9411

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/